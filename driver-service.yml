spring:
  application:
    name: driver
  datasource:
    url: jdbc:postgresql://localhost:5432/driver
    username: postgres
    password: Independent12#

server:
  port: 8081

management:
  endpoint:
    routes:
      enabled: true
    web:
      exposed:
        included: *
